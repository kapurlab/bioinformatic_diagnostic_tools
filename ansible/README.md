# ansible/

Optional `osc.ood`-style role for institutions that manage Open OnDemand with
Ansible: clone the tools from tools.yml, run the installers, register the cards —
idempotently. Status: planned (see docs/SYSADMIN.md).
